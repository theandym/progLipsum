- if flash.any?
	- flash.each do |key, value|
		.flash.clearfix{ :title => key.to_s.humanize, :class => key }
			.content
				.flash_content
					.flash_content_message
						%span= value

- if @object != nil && @object.errors.any?
	.flash.clearfix.error
		.content
			.flash_content
				.flash_content_message
					Oops! Will you fix the
					- if @object.errors.count > 1
						fields
					- else
						field
					in error below?
